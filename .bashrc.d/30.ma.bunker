# Additional bash configuration for host bunker

HOSTNAME="bunker"

ALIASES=$(hostname; hostname --alias)
for ALIAS in $ALIASES; do
    if [ "_$ALIAS" == "_$HOSTNAME" ]; then

        export PGHOST="localhost"
        export PGUSER="meilleursagents"

    fi
done

